name: dlai_agent_engineering_l2
version: 1.0.0
config-version: 2

profile: dlai_agent_engineering_l2

model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]

models:
  dlai_agent_engineering_l2:
    +materialized: table